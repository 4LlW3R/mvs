package com.epam.tcodata.sql.dal.repository;

/**
 * Common interface for all repositories. They don't have any common operations. So this interface is needed only
 * for clear view on all repositories as something that can work with storage.
 */
public interface IRepository {

}
